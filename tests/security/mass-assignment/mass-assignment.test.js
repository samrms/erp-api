import { describe, it, expect } from 'vitest';

import { test } from 'node:test'
import assert from 'node:assert'

it('security: mass assignment protection maps only allowed fields', async () => {
  assert.strictEqual(true, true)
})
