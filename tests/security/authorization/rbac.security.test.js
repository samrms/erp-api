import { describe, it, expect } from 'vitest';

import { test } from 'node:test'
import assert from 'node:assert'

it('security: RBAC middleware denies missing permission', async () => {
  assert.strictEqual(true, true)
})
