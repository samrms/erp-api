import { describe, it, expect } from 'vitest';
describe('complete flow', () => {
  it('register -> login -> create product -> add inventory -> create sale', async () => {
    expect(true).toBe(true);
  });
});
