import { describe, it, expect } from 'vitest';
describe('e2e complete auth flow', () => {
  it('register -> login -> protected endpoint', async () => {
    expect(true).toBe(true);
  });
  it('invalid login rejected', async () => {
    expect(true).toBe(true);
  });
});
