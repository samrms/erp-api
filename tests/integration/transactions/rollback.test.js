import { describe, it, expect } from 'vitest';

import { test } from 'node:test'
import assert from 'node:assert'

it('integration: transaction rollback on error', async () => {
  assert.strictEqual(true, true)
})
